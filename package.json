{
  "name": "jobhunt",
  "version": "1.0.0",
  "private": true,
  "description": "Job hunting dashboard with scraping, tracking, and application management",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "scrape": "npm run scrape --workspace=backend"
  }
}
